# Archived skills

These skills were absorbed from the former `pi-mono` repo into dotfiles on
2026-08-30. Each was an independent git checkout; the nested `.git` dirs were
removed and the files flattened into this repo, so their individual git
history is gone.

They are archived: not actively maintained, kept for reference. Do not add new
work here.
