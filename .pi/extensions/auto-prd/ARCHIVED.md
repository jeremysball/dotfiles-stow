# Archived extension

`auto-prd` was absorbed from the former `pi-mono` repo into dotfiles on
2026-08-30. It was an independent git checkout; the nested `.git` dir was
removed and the files flattened into this repo, so its git history is gone.

It is archived: not actively maintained, kept for reference. Do not add new
work here.
