# Global mise config, symlinked to ~/.config/mise/config.toml by mise's own
# dotfiles feature (see the [dotfiles] table below).
#
# Scope: everything user-space. Pacman keeps only system packages and the
# two exceptions noted below.
#
# Deliberately NOT here:
#   fish     - login shell, must live in /etc/shells
#   ripgrep  - hard dependency of the pacman `opencode` package
#   opencode - pulls ripgrep with it; the two move together or not at all
#   grc      - ships /etc/grc.fish, which config.fish sources by absolute path
#   ghc/cabal/hls - ghcup already manages these well, and manages six GHC
#                   versions side by side. mise installs ghcup, not the toolchain.
#   ttf-nerd-fonts-symbols-mono, discount, texlive-plaingeneric, ispell - Doom
#     Emacs deps with no mise backend at all (fonts, a TeX Live package group,
#     a non-registry CLI, a spell checker). Reproduce via `mise run
#     doom-emacs-extras`.

[tools]
# core CLI, referenced by fish abbrs and aliases
eza    = "latest"
bat    = "latest"
fd     = "latest"
zoxide = "latest"
fzf    = "latest"
jq     = "latest"
yq     = "latest"

# editors
neovim = "latest"

# git
gh      = "latest"
lazygit = "latest"
delta   = "latest"

# runtimes. Pin these per machine in ~/.config/mise/conf.d/ if they need to differ.
node   = "latest"
go     = "latest"
python = "latest"
uv     = "latest"

# llm shell pipe tool. Not in mise's registry under a bare `mods` name, so
# the aqua backend is named explicitly.
"aqua:charmbracelet/mods" = "latest"

# haskell: manage the manager, not the toolchain
ghcup = "latest"

# mise's own shell-completion generator; without it mise prints a warning
# that completions won't work
usage = "latest"

# generic multi-shell completion generator; provides fish completions for
# hundreds of CLIs that don't ship their own
carapace = "latest"

# fun
cowsay = "latest"
# catbow's v1.0.1 release ships cb-v1.0.1-linux-amd64.tar.gz and
# cb-v1.0.1-windows-amd64.zip, so the github: backend can match
# platform-arch asset names without any custom tooling.
"github:jeremysball/catbow" = "latest"

[tasks.serper-axi-install]
description = "Clone (or pull) jeremysball/serper-axi and install its CLI globally. Not published to npm, so this is the only install path. Idempotent: re-running pulls latest and reinstalls."
run = '''
#!/usr/bin/env bash
set -euo pipefail

REPO_DIR="$HOME/projects/serper-axi"

mkdir -p "$HOME/projects"

if [ -d "$REPO_DIR" ]; then
    echo "serper-axi-install: $REPO_DIR already exists, pulling latest"
    git -C "$REPO_DIR" pull
else
    git clone https://github.com/jeremysball/serper-axi.git "$REPO_DIR"
fi

cd "$REPO_DIR"
npm install
npm run build
npm install -g .

echo "serper-axi-install: done. Export SERPER_API_KEY before running serper-axi."
'''

[tasks.secrets-install]
description = "Clone jeremysball/secret-management (scripts) and jeremysball/password-store (encrypted data) if missing, then run secret-management's installer. Idempotent: skips any clone whose target dir already exists."
run = '''
#!/usr/bin/env bash
set -euo pipefail

SCRIPTS_DIR="$HOME/projects/secret-management"
STORE_DIR="$HOME/.password-store"

mkdir -p "$HOME/projects"

if [ -d "$SCRIPTS_DIR" ]; then
    echo "secrets-install: $SCRIPTS_DIR already exists, skipping clone"
else
    git clone https://github.com/jeremysball/secret-management.git "$SCRIPTS_DIR"
fi

if [ -d "$STORE_DIR" ]; then
    echo "secrets-install: $STORE_DIR already exists, skipping clone"
else
    git clone https://github.com/jeremysball/password-store.git "$STORE_DIR"
fi

bash "$SCRIPTS_DIR/install.sh"

echo "secrets-install: done. Run 'secrets-unlock' from a real terminal to decrypt secrets for this session."
'''

[tasks.emacs-install]
description = "GNU Emacs from Arch's repos; the runtime Doom Emacs builds on"
run = "sudo pacman -S --needed emacs"

[tasks.doom-emacs-install]
description = "Clone Doom Emacs source into ~/.config/emacs and run its official installer"
run = "git clone https://github.com/doomemacs/doomemacs ~/.config/emacs && ~/.config/emacs/bin/doom install"

[tasks.doom-emacs-extras]
description = "System packages Doom Emacs needs that mise has no backend for (fonts, TeX Live groups, non-registry CLIs, spell checker)"
# ispell backs Doom's :checkers spell module. Arch ships it in extra, so it
# rides the same pacman line as the rest rather than pulling in brew.
run = "sudo pacman -S --needed ttf-nerd-fonts-symbols-mono discount texlive-plaingeneric ispell"

[tasks.monolisa-nerd-font-patch]
description = "Refresh ~/.local/share/fonts/MonoLisaNerdFontMono-Regular.otf with the current Nerd Fonts icon set"
# The tracked .otf started as mhartington/dotfiles' pre-patched MonoLisa
# Nerd Font (github.com/mhartington/dotfiles, config/fonts/MonoLisa/) --
# last updated there in Feb 2021, five years stale against nerd-fonts'
# active release cadence (v3.5.0 as of this writing). No unpatched MonoLisa
# source is available for free (it's a commercial font from monolisa.dev,
# not owned on this machine), so this re-patches ON TOP of that file with
# --careful (add only what's missing, never touch existing glyphs) rather
# than starting clean. That doubles the family name in font-patcher's own
# output ("MonoLisaNerdFontCompleteMono Nerd Font Mono"), so the last step
# rewrites the name table back to a clean "MonoLisa Nerd Font Mono".
run = '''
#!/usr/bin/env bash
set -euo pipefail

WORK="$HOME/projects/font-patching"

# The base snapshot is a fixed, immutable reference point (mhartington's
# 2021 patch) -- caching it is correct, re-downloading it never gets
# "fresher". FontPatcher is the opposite: it's the whole reason this task
# exists to be re-run, so it's always re-fetched, never cached, or a second
# run would silently keep patching with whatever nerd-fonts release was
# current the first time this task ever ran.
if [ ! -f "$WORK/MonoLisa-Regular-NF-source.otf" ]; then
    curl -fsSL -o "$WORK/MonoLisa-Regular-NF-source.otf" \
        "https://raw.githubusercontent.com/mhartington/dotfiles/refs/heads/main/config/fonts/MonoLisa/MonoLisa%20Regular%20Nerd%20Font%20Complete%20Mono.otf"
fi

rm -rf "$WORK/FontPatcher" "$WORK/patched"
mkdir -p "$WORK/patched"
LATEST_URL=$(curl -s https://api.github.com/repos/ryanoasis/nerd-fonts/releases/latest \
    | jq -r '.assets[] | select(.name == "FontPatcher.zip") | .browser_download_url')
curl -fsSL -o "$WORK/FontPatcher.zip" "$LATEST_URL"
unzip -q -o "$WORK/FontPatcher.zip" -d "$WORK/FontPatcher"

fontforge -script "$WORK/FontPatcher/font-patcher" --complete --careful --mono \
    --outputdir "$WORK/patched" "$WORK/MonoLisa-Regular-NF-source.otf"

PATCHED=$(fd -e otf . "$WORK/patched" | head -1)

cat > "$WORK/fix-name.pe" <<'PE'
Open($1)
SetTTFName(0x409, 1, "MonoLisa Nerd Font Mono")
SetTTFName(0x409, 2, "Regular")
SetTTFName(0x409, 4, "MonoLisa Nerd Font Mono")
SetTTFName(0x409, 6, "MonoLisaNerdFontMono-Regular")
SetTTFName(0x409, 16, "MonoLisa Nerd Font Mono")
SetTTFName(0x409, 17, "Regular")
SetFontOrder(2)
Generate($2)
PE

fontforge -script "$WORK/fix-name.pe" "$PATCHED" \
    "$HOME/.local/share/fonts/MonoLisaNerdFontMono-Regular.otf"

fc-cache -f "$HOME/.local/share/fonts"

echo "monolisa-nerd-font-patch: done. Copy $HOME/.local/share/fonts/MonoLisaNerdFontMono-Regular.otf into this repo's .local/share/fonts/ and commit it to actually update the tracked dotfile."
'''

[settings]
# Read .nvmrc / .python-version / .go-version in repos that never heard of mise
idiomatic_version_file_enable_tools = ["node", "python", "go"]

# Every file this repo manages, symlinked from ~/.dotfiles (the default
# dotfiles.root) into place by `mise bootstrap dotfiles apply`. Every entry
# is one of:
#   - a single file (mode omitted, the default `symlink`)
#   - `mode = "symlink-each"`, for a directory a runtime tool writes into
#     (fisher/tide in fish/functions, fish/conf.d, fish/completions; also
#     .local/bin, since a few of those scripts are dangling symlinks that
#     only resolve on specific machines -- see below). It symlinks only the
#     files that exist in the repo's copy of the directory, so anything a
#     tool writes straight into the live directory is left alone, and a
#     source that doesn't resolve on this machine is skipped rather than
#     failing the whole apply the way a plain `= {}` entry would.
#   - `mode = "symlink"` on a whole directory, for the four git submodules
#     (alacritty-theme, zsh-autosuggestions, zsh-syntax-highlighting,
#     powerlevel10k), since nothing writes into an external theme/plugin
#     checkout at runtime. Submodules aren't checked out by a plain
#     `git clone` though -- run `git submodule update --init --recursive`
#     first (init.sh does this for you), or those four entries symlink
#     empty directories.
#
# A machine that does not want a given target (the graphical-workstation
# section below, for instance) just does not need those entries applied --
# either strip them locally before applying, or leave them: an unapplied
# entry with no source machine state does nothing until you `mise bootstrap
# dotfiles add` it for real.
[dotfiles]
# fish. completions/conf.d/functions use symlink-each: it symlinks only the
# files that exist in the repo's copy of each directory, so a file fisher or
# tide writes directly into the live directory (never into the repo) is left
# alone untouched. Same guarantee as one entry per file, far fewer lines, and
# a new file added to one of these three directories is picked up on the next
# apply without an extra table edit.
"~/.config/fish/completions" = { mode = "symlink-each" }
"~/.config/fish/conf.d" = { mode = "symlink-each" }
"~/.config/fish/functions" = { mode = "symlink-each" }
"~/.config/fish/config.fish" = {}
"~/.config/fish/fish_plugins" = {}

# mise (this file too)
"~/.config/mise/config.toml" = {}

# nvim
"~/.config/nvim/.gitignore" = {}
"~/.config/nvim/.neoconf.json" = {}
"~/.config/nvim/LICENSE" = {}
"~/.config/nvim/README.md" = {}
"~/.config/nvim/init.lua" = {}
"~/.config/nvim/lazy-lock.json" = {}
"~/.config/nvim/lazyvim.json" = {}
"~/.config/nvim/lua/config/autocmds.lua" = {}
"~/.config/nvim/lua/config/keymaps.lua" = {}
"~/.config/nvim/lua/config/lazy.lua" = {}
"~/.config/nvim/lua/config/options.lua" = {}
"~/.config/nvim/lua/plugins/99.lua" = {}
"~/.config/nvim/lua/plugins/example.lua" = {}
"~/.config/nvim/stylua.toml" = {}

# doom
"~/.config/doom/config.el" = {}
"~/.config/doom/init.el" = {}
"~/.config/doom/packages.el" = {}

# bin scripts. symlink-each also means gf2/libregate/pico8/speedcat (dangling
# symlinks to tool paths that only exist on the machine that has those
# tools -- see the [dotfiles] header comment) are silently skipped on a
# machine that doesn't have them, instead of failing the whole apply the way
# an individual `= {}` entry does.
"~/.local/bin" = { mode = "symlink-each" }

# fonts. MonoLisaNerdFontMono-Regular.otf is the patched terminal/editor font
# itself, not a fontconfig setting -- reproduce or refresh it via
# `mise run monolisa-nerd-font-patch`.
"~/.config/fontconfig/fonts.conf" = {}
"~/.local/share/fonts/MonoLisaNerdFontMono-Regular.otf" = {}

# bash
"~/.bashrc" = {}

# git
"~/.config/git/ignore" = {}
"~/.gitconfig" = {}

# gh
"~/.config/gh/config.yml" = {}
"~/.config/gh/pat-switcher.sh" = {}

# systemd
"~/.config/systemd/user/bumblebee-nightly.service" = {}
"~/.config/systemd/user/bumblebee-nightly.timer" = {}

# zellij, htop, superfile, himalaya, alfred, taskferry, mcporter
"~/.config/alfred/config.toml" = {}
"~/.config/himalaya/config.toml" = {}
"~/.config/htop/htoprc" = {}
"~/.config/mcporter/config.json" = {}
"~/.config/superfile/compact.toml" = {}
"~/.config/superfile/config.toml" = {}
"~/.config/superfile/hotkeys.toml" = {}
"~/.config/taskferry/config.json" = {}
"~/.config/zellij/config.kdl" = {}

# older graphical workstation: i3, xorg, picom, alacritty, helix, zsh, tmux
"~/.config/alacritty/alacritty-theme" = {}  # submodule, whole-dir symlink
"~/.config/alacritty/alacritty.toml" = {}
"~/.config/helix/config.toml" = {}
"~/.config/helix/ignore" = {}
"~/.config/helix/languages.toml" = {}
"~/.config/i3/config" = {}
"~/.config/picom.conf" = {}
"~/.tmux.conf" = {}
"~/.xinitrc" = {}
"~/.xprofile" = {}
"~/.zsh/aliases" = {}
"~/.zsh/custom/plugins/zsh-autosuggestions" = {}  # submodule, whole-dir symlink
"~/.zsh/custom/plugins/zsh-syntax-highlighting" = {}  # submodule, whole-dir symlink
"~/.zsh/custom/themes/linuxonly-purple.zsh-theme" = {}
"~/.zsh/custom/themes/powerlevel10k" = {}  # submodule, whole-dir symlink
"~/.zsh/functions/e" = {}
"~/.zsh/functions/edit.sh" = {}
"~/.zshrc" = {}
"~/.zshrc.local" = {}

# haskell (cabal, ghcup config -- the ghc toolchain itself stays unmanaged
# per the [tools] header comment, ghcup owns it)
"~/.config/cabal/config" = {}
"~/.config/ghcup/config.yaml" = {}

# bumblebee-nightly (config only; the systemd service/timer pair is tracked
# above alongside bumblebee-nightly.{service,timer})
"~/.config/bumblebee-nightly/config" = {}

# herdr -- config.toml only. herdr-client.log, herdr-server.log,
# session.json, release-notes.json are runtime state, not config, and
# deliberately not tracked here.
"~/.config/herdr/config.toml" = {}

# mods
"~/.config/mods/mods.yml" = {}

# playwright-mcp
"~/.config/playwright-mcp/config.json" = {}

# tmux -- local override layered on ~/.tmux.conf above
"~/.config/tmux/tmux.conf.local" = {}

# opencode -- config only. meta-api-key is a live credential; node_modules,
# package.json/package-lock.json are installed deps; backups-2026-07-17/ is
# a stale snapshot. None of those four are tracked.
"~/.config/opencode/opencode.json" = {}
"~/.config/opencode/opencode.jsonc" = {}
"~/.config/opencode/tui.json" = {}
"~/.config/opencode/agent/hermes.md" = {}
"~/.config/opencode/agents/notools-luna.md" = {}
"~/.config/opencode/agents/notools-sol.md" = {}
"~/.config/opencode/agents/notools-terra.md" = {}
"~/.config/opencode/plugins/herdr-agent-state.js" = {}

